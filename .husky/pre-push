# .husky/pre-push

CHANGED=$(git diff --name-only origin/$(git branch --show-current))

echo "$CHANGED" | grep -q "^server/" && npm run build:server || true
echo "$CHANGED" | grep -q "^client/" && npm run build:client || true
